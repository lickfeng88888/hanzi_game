{
  "name": "hanzi-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve .",
    "build": "mkdir -p public && cp -r * public/ || true",
    "start": "serve public"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
} 